import { StyleSheet } from "react-native";

export const s=StyleSheet.create({
    root:{
        flex:1,
        justifyContent:'space-evenly',
        flexDirection:"row",
        alignItems:'center',
    }
});